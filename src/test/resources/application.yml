spring:
  config:
    activate:
      on-profile: integration-test
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;
    username: sa
    password: sa
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    show-sql: true
    open-in-view: false

server:
  port: 8080

security:
  jwt:
    expiration-in-minutes: 10
    secret-key: na

mock:
  enabled: false